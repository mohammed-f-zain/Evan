# Copy to .env and set strong values before first start.
POSTGRES_USER=odoo
POSTGRES_PASSWORD=change_me
ODOO_ADMIN_PASSWORD=change_me
# Optional: only for local dev if port 8069 is already in use
# ODOO_HTTP_PORT=8079
