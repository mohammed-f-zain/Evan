# Copy to .env and set strong values before first start.
POSTGRES_USER=odoo
POSTGRES_PASSWORD=change_me
ODOO_ADMIN_PASSWORD=change_me
# Host port mapped to Odoo (8079 if 8069 is already used locally)
ODOO_HTTP_PORT=8079
